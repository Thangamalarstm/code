#include <stdio.h>
#include<string.h>
int main()
{
   char a[100], c;
   int i;

   printf("Enter a string: ");
   scanf();

   printf("Enter a character: ");
   scanf("%s",&c);

   for(i = 0;a[i] != '\0'; ++i)
   {
       if(c == str[i])
          
   }

   printf(" %s = %d", c);

   return 0;
}
