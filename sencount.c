#include<stdio.h>
#include<string.h>
int main()
{
int i,ic;
char a[100]="good.best;
printf("Enter the sentence:");
scanf("%d",&a[i]);
for(i=1;a[i]='/0';i++)
{
if(a[i]==".")
ic++;
}
printf("Count of the given sentence is:%d",ic+1);
return 0;
}
