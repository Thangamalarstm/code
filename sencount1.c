#include<stdio.h>
#include<string.h>
int main()
{
int i,c;
char a[100]="good.best;
printf("Enter the sentence:");
scanf("%d",&a[i]);
for(i=1;a[i]='/0';i++)
{
if(a[i]==".")
c++;
}
printf("Count of the given sentence is:%d",c+1);
return 0;
}
